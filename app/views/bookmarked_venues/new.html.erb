<!-- Validation failure messages -->
<% if @bookmarked_venue.errors.any? %>
  <% @bookmarked_venue.errors.full_messages.each do |message| %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <%= message %>
    </div>
  <% end %>
<% end %>

<div class="page-header">
  <h3>Add Bookmarked Venue</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/create_bookmarked_venue" method="post">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for dish_id -->
      <div class="form-group">
        <label for="dish_id" class="control-label">
          Dish
        </label>

        <%= select_tag(:dish_id, options_from_collection_for_select(DishList.all, :id, :dish_names, @bookmarked_venue.dish_id), :class => "form-control") %>
      </div>

      <!-- Label and input for venue_id -->
      <div class="form-group">
        <label for="venue_id" class="control-label">
          Venue
        </label>

        <%= select_tag(:venue_id, options_from_collection_for_select(VenueDetail.all, :id, :venue_name, @bookmarked_venue.venue_id), :class => "form-control") %>
      </div>

      <button class="btn btn-success">
        Create Bookmarked Venue
      </button>
      or
      <a href="/bookmarked_venues">Cancel</a>
    </form>
  </div>
</div>
